package com.hrms.dao;

import javax.persistence.EntityManager;
import org.apache.log4j.Logger;

public class AdminUserDAO extends GenericDAO {

    private static final Logger logger = Logger.getLogger(AdminUserDAO.class);

    public AdminUserDAO(EntityManager entityManager) {
        super(entityManager);
        logger.debug("AdminUserDAO Initializing...");
    }
}
